import type { Metadata } from "next";

export const metadata: Metadata = { title: "Privacy", description: "How the current Museion build handles learner and source data." };

export default function PrivacyPage() {
  return <article className="mx-auto w-full max-w-3xl px-4 py-16 sm:px-6 lg:py-24"><p className="eyebrow">Current build · July 2026</p><h1 className="mt-4 font-display text-5xl font-semibold tracking-[-0.04em]">Privacy, in plain language.</h1><p className="mt-6 max-w-[62ch] text-lg leading-8 text-ink-soft">Museion is currently a local demonstration build. It uses an anonymous HTTP-only cookie to keep a learner’s sessions separate. It does not require a name, email address, or account.</p><div className="mt-12 space-y-10"><section><h2 className="font-display text-2xl font-semibold">What the app holds</h2><p className="mt-3 leading-7 text-ink-soft">Lesson state, concept progress, attempts, hint counts, and tutor-event metadata live in server memory. Judge transfer answers are hashed before evidence is stored. Restarting the server clears in-memory records.</p></section><section><h2 className="font-display text-2xl font-semibold">Source material</h2><p className="mt-3 leading-7 text-ink-soft">Text, Markdown, and selectable-text PDFs are normalized in the browser for inspection. The checked replay is built from a repository fixture. Do not upload material you are not authorized to use.</p></section><section><h2 className="font-display text-2xl font-semibold">Live AI</h2><p className="mt-3 leading-7 text-ink-soft">Without a configured server-side OpenAI key, Maia uses deterministic fallback and the golden compiler uses replay. A future hosted deployment must publish its provider retention, hosting, and deletion terms before accepting real learner data.</p></section><section className="rounded-2xl bg-gold-soft p-6"><h2 className="font-display text-xl font-semibold">Before production</h2><p className="mt-2 leading-7 text-ink-soft">This notice must be replaced with deployment-specific controller identity, lawful basis, retention periods, subprocessors, contact details, and applicable learner/minor safeguards.</p></section></div></article>;
}
