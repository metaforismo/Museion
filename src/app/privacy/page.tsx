import type { Metadata } from "next";

export const metadata: Metadata = { title: "Privacy", description: "How the current Museion build handles learner and source data." };

export default function PrivacyPage() {
  return <article className="mx-auto w-full max-w-3xl px-4 py-16 sm:px-6 lg:py-24"><p className="eyebrow">Current build · July 2026</p><h1 className="mt-4 font-display text-5xl font-semibold tracking-[-0.04em]">Privacy, in plain language.</h1><p className="mt-6 max-w-[62ch] text-lg leading-8 text-ink-soft">Museion is a local-first demonstration. It uses an anonymous HTTP-only cookie to separate learners and does not require a name or email address.</p><div className="mt-12 space-y-10"><section><h2 className="font-display text-2xl font-semibold">What the app holds</h2><p className="mt-3 leading-7 text-ink-soft">Authored lesson sessions and concept profiles currently use bounded process memory. Compiler and Judge records can use either bounded memory or the configured Supabase state adapter. Judge transfer answers are hashed before evidence is stored. The active storage mode is a deployment setting, so a local restart can clear memory-backed records.</p></section><section><h2 className="font-display text-2xl font-semibold">Source material</h2><p className="mt-3 leading-7 text-ink-soft">Text, Markdown, and selectable-text PDFs are normalized in the browser for inspection, then sent to the local compiler only when you choose Compile. Do not upload material you are not authorized to use.</p></section><section><h2 className="font-display text-2xl font-semibold">ChatGPT and live AI</h2><p className="mt-3 leading-7 text-ink-soft">Local live mode asks the official Codex runtime to use GPT-5.6 models through your connected ChatGPT account. Museion does not read or receive the OAuth token. Requests use ChatGPT/Codex plan quota. If the runtime is unavailable, Maia visibly uses verified deterministic guidance and arbitrary-source compilation stops without starting paid API usage.</p></section><section className="rounded-2xl bg-gold-soft p-6"><h2 className="font-display text-xl font-semibold">Before production</h2><p className="mt-2 leading-7 text-ink-soft">A hosted production service must publish controller identity, lawful basis, retention periods, subprocessors, contact details and learner/minor safeguards before accepting real learner data.</p></section></div></article>;
}
