# Required for Maia's live tutoring (LLM coaching layer).
# Without it, Museion still runs: lessons, verification, mastery and the
# deterministic hint ladder all work offline.
ANTHROPIC_API_KEY=sk-ant-...
